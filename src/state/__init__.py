# This module contains a the representation of a state.
# The optimization algorithms are agnostic to the type of state, which means that if you implement a state for a different 
# problem, using functions with the same names, the optimization algorithms will work out of the box.